[project]
name = "doc-gen"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "markitdown>=0.1.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
